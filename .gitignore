__pycache__/
*.pyc
.venv/
*.egg-info/

# monitor.py's rolling state cache -- a full copy of the last-seen spec per
# watched API, regenerated every check. Not something to version; the run
# records under reports/monitor_runs/ are the durable evidence of what it found.
data/state/
