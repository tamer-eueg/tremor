__pycache__/
*.pyc
.venv/
*.egg-info/

# monitor.py's rolling state cache (gzip-compressed, ~460KB per API rather
# than the ~7-13MB raw spec) IS committed on purpose as of phase 7 -- it's
# the durable baseline the scheduled GitHub Actions job diffs the next
# check against, with no database or hosting needed. Not excluded here.
